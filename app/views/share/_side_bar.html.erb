<div class="sideBar">
            <ul>
                <%= link_to  "探す", root_path %>
                <li><a href="#">相手から</a></li>
                <%= link_to  "チャット", rooms_path(current_user) %>
            </ul>
            
                <%= link_to user_match_path(user_id: current_user.id, id: current_user.id) do %>
                    <i class="bi bi-person-circle"></i>
                <% end %>
</div>